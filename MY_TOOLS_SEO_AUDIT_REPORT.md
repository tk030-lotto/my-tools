# My Tools SEO現状調査レポート

**対象サイト**: [https://tk030-lotto.github.io/my-tools/](https://tk030-lotto.github.io/my-tools/)  
**リポジトリ**: [https://github.com/tk030-lotto/my-tools](https://github.com/tk030-lotto/my-tools)  
**調査実施日**: 2026-09-09  
**調査方針**: 「今あるMy Toolsを壊さない。作り直さない。SEOのために必要な技術的不足だけを見つける。」  
**掲載ツール数**: 全20件（掲載基準改定により「ロト＆ナンバーズ 統合構造解析・出目表可視化ツール」を掲載対象に追加）

---

## 1. エグゼクティブサマリー（現状評価）

現在の「My Tools」は、Vanilla HTML/CSS/JavaScriptによる軽量・Zero-Dependencyな設計となっており、表示速度やモバイル表示の品質、視認性の高いUIデザインはすでに高い水準にあります。

しかし、**SEO（Google検索エンジンによるクロール・理解・インデックス）の観点では、構造的な大きな課題が2点存在します。**

1. **初期HTMLにおけるツールコンテンツの完全欠落（完全CSR構造）**:
   掲載されている全20ツールの「ツール名」「説明文」「タグ」「各ツールへのリンク（Web版/GitHub/note）」がすべて `data/tools.json` からJavaScriptで動的に生成されています。初期HTMLの段階ではカードが0件であり、GooglebotがJavaScriptを実行する前の第1ウェーブ（First Wave）では「タイトルとヘッダーの数行しかないページ」として処理されます。
2. **メタデータおよびインデックス制御要素の不足・未最適化**:
   `canonical` タグが存在せず、`robots.txt` および `sitemap.xml` が未配置です。また、`<title>` が「My Tools — 面倒だったから、作ってみた。」となっており、具体的な検索クエリ（「AI開発支援」「無料Webツール」「ブラウザ完結」等）が含まれていません。

既存UI・デザイン・JavaScript機能への変更を行わず、初期HTMLへの構造化データ（JSON-LD）提供およびメタタグの適正化を行うことで、これらの課題は低コストかつ安全に改善可能です。

---

## 2. 技術構成とサイト構造の調査結果

### 2.1 技術構成

| 項目 | 現状の実装 | 技術的評価 |
| :--- | :--- | :--- |
| **フレームワーク** | なし（Vanilla HTML5, CSS3, ES6 JavaScript） | Zero-Dependency。軽量で高速動作 |
| **ビルドツール** | なし（ノービルド構成） | ビルド設定不要で運用シンプル |
| **サイト種別** | クライアントサイドSPA的カタログサイト（1ページ完結） | 単一 `index.html` から全機能を提供 |
| **ホスティング/デプロイ** | GitHub Pages（`.nojekyll` 配置、静的直接配信） | 高速かつ安定したCDN配信 |
| **データ管理** | `data/tools.json`（全20ツールのマスターデータ） | JSON管理により保守性が高い |
| **コンテンツ生成方式** | **完全クライアントサイドレンダリング（CSR）** | `fetch('data/tools.json')` 後にDOM挿入 |

### 2.2 ページ構造（index.html）

- **`<title>`**: `My Tools — 面倒だったから、作ってみた。`（行 6）
- **`<meta name="description">`**: `「面倒だったから、作ってみた。」AIと一緒に作った公開ツールのカタログサイトです。日常の面倒を減らすツールやAI開発支援ツールをまとめています。`（全角76文字、行 7）
- **`<meta name="robots">`**: 未指定（未指定時はデフォルトで `index, follow` とみなされる）
- **`canonical`**: **未設定（タグ自体が存在しない）**
- **OGP**:
  - `og:title`: 設定あり
  - `og:description`: 設定あり
  - `og:type`: `website`
  - `og:image`: 設定あり
  - `twitter:card`: `summary_large_image`
  - `og:url`: **未設定**
- **Favicon**: `<link rel="icon" href="favicon.ico" type="image/x-icon">`（ルートに `favicon.ico` 存在）
- **見出し構造**:
  - `<h1>`: `<h1 class="site-title">My Tools</h1>`（ページ内に1つ、適切）
  - `<h2>`: 初期HTMLには0件。JS実行後にカード内の `<h2 class="card-title">` として動的生成。
  - `<h3>`: `<div id="emptyState">` 内の文言（初期状態では `display: none`）
- **本文（初期HTML）**:
  - ヘッダー部分のリード文・説明文（約120文字）およびフッターのみ。
  - 20個のツール名、説明文、カテゴリ、タグ、リンクは初期HTML内に一切存在しない。
- **内部・外部リンク**:
  - 初期HTML内に存在するリンク: noteマガジンリンク、GitHub Profileリンクの2件のみ。

### 2.3 公開ファイルの有無

| ファイル | 存在有無 | 検索エンジンへの影響 |
| :--- | :--- | :--- |
| **`robots.txt`** | **なし** | 404応答となるためクロール拒否は起きないが、sitemapの所在通知ができない |
| **`sitemap.xml`** | **なし** | 単一ページのため致命的ではないが、正規URLの明示・Google Search Consoleへの登録に不足 |
| **`404.html`** | なし | 存在しないURLへのアクセス時にGitHub標準404が表示される |
| **`manifest.json`** | なし | PWA用途ではないためSEO上の直接的な悪影響はなし |

---

## 3. 最重要調査：Googleがツール情報を認識できるか（JavaScriptの影響）

### 3.1 ツール一覧の生成フロー

現在のサイトにおけるコンテンツ生成フローは以下の通りです。

```text
【ブラウザ/Googlebotのアクセス】
       │
       ▼
[初期HTMLの受信 (index.html)]
  ※この時点では <div id="toolGrid"></div> は空
       │
       ▼
[js/app.js の読み込みと実行]
       │
       ▼
[fetch('data/tools.json') の非同期通信]
       │
       ▼
[JSONデータ受信・パース]
       │
       ▼
[toolGrid.innerHTML による20個のカードDOM生成]
```

### 3.2 Googlebotが初期HTMLだけを取得した場合の差分

GooglebotがJavaScriptを実行する前の初期HTMLと、JavaScript実行後のDOMの差分を検証しました。

| 情報項目 | 初期HTML（JS実行前） | レンダリング後（JS実行後） |
| :--- | :---: | :---: |
| サイトタイトル（My Tools） | ◯ 取得可能 | ◯ 取得可能 |
| サイト説明文（リード文） | ◯ 取得可能 | ◯ 取得可能 |
| **20個のツール名** | **× 取得不可（0件）** | ◯ 取得可能（20件） |
| **各ツールの説明文 (description)** | **× 取得不可** | ◯ 取得可能 |
| **各ツールのカテゴリ・タグ** | **× 取得不可** | ◯ 取得可能 |
| **各ツールのWeb版URL (GitHub Pages)** | **× 取得不可** | ◯ 取得可能 |
| **各ツールのGitHubリポジトリURL** | **× 取得不可** | ◯ 取得可能 |
| **各ツールのnote記事URL** | **× 取得不可** | ◯ 取得可能 |
| **ツールの詳細情報 (Why / Features)** | **× 取得不可** | **× 取得不可（※後述）** |

> **※モーダル内の詳細情報（Why / Features）について**:  
> 各ツールの「② なぜ作ったか (Why)」「③ 何ができるか (Features)」は、ユーザーがカード上の「詳細・解説」ボタンをクリックした際に、関数 `window.showToolDetail(toolId)` が呼び出されて初めて `#modalBody` に挿入されます。検索エンジンのクローラーはボタンのクリックイベントを実行しないため、WhyやFeaturesに含まれる詳細な説明・機能一覧テキストはGoogleに認識されません。

### 3.3 Google検索に対するJavaScriptの影響分析

1. **2段階インデックス（Two-wave Indexing）による遅延**:
   Googlebotは初期HTMLを取得して即座に第1段階の処理（First Wave）を行い、レンダリングリソース（WRS: Web Rendering Service）に空きができてから第2段階のJavaScriptレンダリング（Second Wave）を実行します。このタイムラグにより、新規公開ツールや更新内容のインデックス反映が遅れる原因となります。
2. **リソース取得制限（クロールバジェット）とタイムアウト**:
   Googlebotが `data/tools.json` の取得をスキップしたり、レンダリング制限時間（一般に数秒以内）を超過した場合、ページは「ツールが0件の空ページ」としてインデックスされるリスクを孕んでいます。
3. **他検索エンジン・SNSクローラー・AIクローラーの制限**:
   Googlebot以外の検索エンジンや各種ソーシャルプレビュー、LLM検索クローラーは、JavaScriptをレンダリングしないか極めて限定的にしか解釈しません。そのため、初期HTMLにテキストが存在しない場合、外部からは「空のカタログ」と評価されます。

---

## 4. 各種SEO基本要素の詳細診断

### 4.1 title調査

- **現在のtitle**:
  `My Tools — 面倒だったから、作ってみた。`
- **問題点**:
  1. 「My Tools」はあまりにも一般的・抽象的な単語であり、固有性（識別性）が低い。
  2. キャッチコピー「面倒だったから、作ってみた。」は検索キーワードとしての検索意図とは乖離している。
  3. ページの実態である「AI開発支援」「無料Webツール」「公開ツールカタログ」といった検索需要のある具体的キーワードが1つも含まれていない。
- **改善案**:
  `My Tools — AI開発支援・無料Webツール一覧カタログ`
  （実用性と具体的検索キーワードに特化）

### 4.2 meta description調査

- **現在のdescription**:
  `「面倒だったから、作ってみた。」AIと一緒に作った公開ツールのカタログサイトです。日常の面倒を減らすツールやAI開発支援ツールをまとめています。`（全角76文字）
- **改善案**:
  `日常の面倒を減らす無料Webツール＆AI開発支援ツールの公開カタログ。「引き継いでみよう」「エラーで止まらない」「CookScale」など、ブラウザですぐ使える個人開発ツールをまとめています。ソースコード（GitHub）と開発ストーリー（note）も公開中。`（約120文字）

### 4.3 canonical調査

- **現在の状況**: `<link rel="canonical" ...>` が存在しない。
- **改善案**:
  `<link rel="canonical" href="https://tk030-lotto.github.io/my-tools/">` を `<head>` に設置する。

### 4.4 robots.txt調査

- **現在の状況**: 未配置。
- **改善案**:
  クロール許可とサイトマップ所在を明記したシンプルな `robots.txt` をルートに配置する。

### 4.5 sitemap.xml調査

- **現在の状況**: 未配置。
- **作成要否の判断**:
  本サイトは1ページ構成であるため、トップページ1URLのみを明記した最小構成の `sitemap.xml` を配置する。ツール増加時は `lastmod`（更新日）のみを自動同期する。

### 4.6 構造化データ（Schema.org JSON-LD）

- **現在の状況**: 未設定（0件）。
- **選定方針**:
  `WebSite` および ツール一覧を表す `ItemList` を初期HTMLの `<script type="application/ld+json">` に配置する。
  `tools.json` を情報源とし、スクリプト（`scripts/update_seo.py`）により機械可読な構造化データを自動生成・同期する。

---

## 5. 個別ページの必要性検討（A案 / B案 / C案の比較）

| 比較項目 | A案：現在の1ページ構成を維持 | B案：各ツールの個別ページを追加 | C案：サイト構造の大幅刷新 |
| :--- | :--- | :--- | :--- |
| **概要** | 現行のカタログ型1ページを維持し、メタデータや初期HTMLを最適化 | 個別ページを20件作成 | Next.jsやSSGフレームワーク等へ全面移行 |
| **判定** | **【強く推奨】** | **【非推奨】** | **【非推奨】** |
| **理由** | ドメイン評価が集約され、保守が極めて容易。note記事との重複も起きない | 各ツールのGitHub Pages本体やnote記事と3重の重複コンテンツを起こす | Zero-Dependency方針に反し保守負荷が増大 |

---

## 6. SEO現状評価表（全16項目）

評価基準: `問題なし` / `軽微な改善` / `改善推奨` / `要対応`

| # | 項目 | 現状 | 評価 | 改善必要性 |
| :-: | :--- | :--- | :---: | :---: |
| 1 | **title** | `My Tools — 面倒だったから、作ってみた。` | **要対応** | 高 |
| 2 | **description** | 76文字。具体ツール名やユーザー利点が不足 | **改善推奨** | 中 |
| 3 | **canonical** | 未設定（タグなし） | **要対応** | 高 |
| 4 | **robots.txt** | 未配置（404応答） | **軽微な改善** | 中 |
| 5 | **sitemap** | 未配置 | **軽微な改善** | 中 |
| 6 | **H1** | `<h1 class="site-title">My Tools</h1>`（1つのみ） | **改善推奨** | 中 |
| 7 | **見出し構造** | 初期HTMLはH1のみ。H2はJSで動的生成 | **改善推奨** | 中 |
| 8 | **本文** | ヘッダー・フッターのみ（約120文字） | **要対応** | 高 |
| 9 | **ツール名** | 全20件がJS動的生成。初期HTMLに0件 | **要対応** | 高 |
| 10 | **ツール説明** | 全20件がJS動的生成。初期HTMLに0件 | **要対応** | 高 |
| 11 | **内部リンク** | 各ツールへのリンクはJS実行後にのみ出現 | **改善推奨** | 中 |
| 12 | **JavaScript** | コンテンツの100%がクライアントサイド非同期描画 | **改善推奨** | 中〜高 |
| 13 | **OGP** | 基本タグあり。`og:url` のみ欠落 | **軽微な改善** | 低 |
| 14 | **構造化データ** | 未設定（JSON-LDなし） | **改善推奨** | 中 |
| 15 | **モバイル対応** | レスポンシブ対応済み（メディアクエリあり） | **問題なし** | なし |
| 16 | **ページ速度** | Zero-Dependency、外部依存なし、極めて高速 | **問題なし** | なし |

---

## 7. 推奨実装範囲（最小構成）

既存UI・デザイン・JavaScript機能への変更を行わず、以下の最小構成で対応します。

1. **`data/tools.json`**: 掲載基準改定に伴う新規ツールの追加（全20ツール）
2. **`scripts/update_seo.py` の作成**: `tools.json` を原本として `index.html` の JSON-LD（`ItemList`）および `sitemap.xml` の更新日を自動同期
3. **`index.html` の `<head>` 改修**: `<title>`、`<meta name="description">`、`<link rel="canonical">`、`<meta property="og:url">` の設定
4. **`robots.txt` / `sitemap.xml` の配置**:
5. **`scripts/sync_tools.py` への自動連動**: `記事同期.bat` を実行するだけで画面表示データとSEOデータが自動更新される仕組みの確立
